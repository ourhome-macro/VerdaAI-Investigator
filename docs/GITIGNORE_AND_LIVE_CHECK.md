# Git 忽略规则与本地联调检查（2026-09-17）

## 忽略规则

项目 `.gitignore` 已覆盖 `.env`、`.venv`、`node_modules`、`dist`、数据库和运行日志。本次补充 `.env.*`（保留 `.env.example`）、常见测试与分析缓存、Vite 缓存，以及不参与应用构建的根目录比赛展示稿。

验证结果：`backend/.env`、虚拟环境、前端依赖、构建输出、运行日志和展示稿均被忽略；`backend/.env.example` 与 `api/.env.example` 保持可提交。当前项目目录下仍有约 177 个未跟踪的源码、静态资源和文档文件，约 5.77 MiB。

## Git 根目录注意

当前 Git 根目录是 `E:/githubDemos`，当前项目是其下的 `multi agent/` 未跟踪目录；父目录原有同名项目文件显示为删除。直接在父目录执行 `git add -A` 会把父目录删除项和其他项目一起纳入暂存，这不是 `.gitignore` 能修复的目录归属问题。提交当前项目时应明确限定路径，或先确认仓库应以哪个目录为根。

## 服务状态

前端 `127.0.0.1:3400` 与后端 `127.0.0.1:8010` 已启动。后端检测到 DeepSeek 配置，真实 `/api/llm/ping` 调用成功，返回模型 `deepseek-flash`。随后填写博查 Key 并重启后端，经前端代理请求 `/api/search?q=DeepSeek&num=1` 成功返回 1 条结果。经前端代理创建 quick 调研任务并提交澄清答案成功。完整 SSE 报告生成尚未执行。
