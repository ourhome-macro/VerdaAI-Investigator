# 仓库关联与提交记录

日期：2026-09-23。

- 目标仓库：`https://github.com/ourhome-macro/VerdaAI-Investigator.git`。
- 本地分支：`main`；目标分支：`origin/main`。
- 该远程是用户指定的 fork；本次不向上游仓库推送，也不创建 PR。
- 本地仓库此前独立初始化，与远程没有共同祖先。采用保留双方提交历史的合并，不使用强制推送；保留远程已有的比赛演示稿。
- 本次整理应用配置、访客隔离、部署文件、来源治理、双维度可信度、最终审校、后台执行、测试和案例文档，保证关联仓库后的源码可完整运行。
- `.env`、真实 API Key、本地数据库、日志、虚拟环境、依赖目录和构建输出不纳入提交；提交前检查暂存区及本地新增历史中的敏感信息。

中文提交说明：`完善来源治理、双维度可信度与新能源车联调验收`。

合并后执行回归检查，采用普通推送更新该 fork 的 `main`，并核对远端提交哈希。

实际记录：

- 功能提交：`b33ac55`，`完善来源治理、双维度可信度与新能源车联调验收`。
- 待合并远端头：`c6d6bfbe4da75bb506fa1de7f742d9db5e92dc56`。
- 合并差异仅为原有演示稿和 `restart.sh`、`stop.sh` 的可执行位，应用源码保持功能提交版本。
- 演示稿与本地既有文件内容哈希一致，没有覆盖不同版本的本地素材。
- 暂存区与本地新增历史的密钥检查通过；54 项后端测试、前端生产构建和 ESLint 通过。前端仍有已有的大 chunk 提示。
